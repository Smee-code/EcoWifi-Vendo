fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
# Logo processing: resizing uploads and making white backgrounds
# transparent. Wheels exist for arm64, so this still installs on an
# Orange Pi without a compiler.
Pillow>=10.0

